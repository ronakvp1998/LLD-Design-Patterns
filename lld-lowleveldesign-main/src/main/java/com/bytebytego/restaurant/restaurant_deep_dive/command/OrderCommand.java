package com.bytebytego.restaurant.restaurant_deep_dive.command;

// Interface for the Command pattern that defines the contract for all order-related commands
public interface OrderCommand {
    void execute();
}
